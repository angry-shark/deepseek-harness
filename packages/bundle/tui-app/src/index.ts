/**
 * @deepseek-ai/dsh-tui-app — the interactive terminal profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the
 * `dsh.bundle.patch` manifest field and resolved by the profile composer
 * through that field; the `tui` row it inserts is the
 * `@deepseek-ai/dsh-tui` front door. This module carries no runtime API.
 * @module @deepseek-ai/dsh-tui-app
 */

export {}
