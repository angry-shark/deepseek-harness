{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "window-controls",
  "description": "Window-control permissions for the custom titlebar. The GUI is served by the harness over http://127.0.0.1, so the capability scopes these to that remote origin only: the renderer can minimize/maximize/close and drag its undecorated window, but nothing else on the system is reached.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "permissions": [
    "core:window:default",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-is-maximized",
    "core:window:allow-close",
    "core:window:allow-start-dragging",
    "core:window:allow-set-title"
  ]
}
